import  { CheckCircle as CheckCircleIcon } from 'lucide-react';

const CheckCircle = ({ className = "h-6 w-6 text-green-500 mr-3" }) => {
  return <CheckCircleIcon className={className} />;
};

export default CheckCircle;
 